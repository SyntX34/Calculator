feat: cross-platform build system, mobile responsiveness, and GitHub release workflow

- Add GitHub Actions workflow (.github/workflows/release.yml) that builds for
  Windows, macOS, Linux (via Tauri), Android, iOS (via Capacitor), and Web
  on manual dispatch with version input and auto-generated changelog
- Add build-static.php: pre-renders PHP templates to static HTML in dist/
  for Tauri/Capacitor packaging (SPA with JS routing, preserves DOM event listeners)
- Add scripts/generate-icons.php: generates app icons from logo.svg or
  creates a calculator icon fallback using GD
- Add Tauri v1 config (src-tauri/) for optimized desktop builds (~3MB binaries)
- Add Capacitor config (capacitor.config.json) for iOS/Android mobile builds
- Add package.json with build scripts (build:static, build:tauri, etc.)
- Update .gitignore with build artifact directories
- Make UI fully responsive: 480px and 380px breakpoints, safe-area-inset
  support for notched phones, iOS zoom prevention (font-size: 16px on inputs),
  touch-action: manipulation to eliminate 300ms tap delay, 44px min touch targets
- Add nav toggle button for mobile page navigation (Calculator/About/Contact)
- Fix: sidebar toggle now only operates on mobile screens (was creating
  transparent overlay on desktop with no sidebar movement)
- Fix: sidebar toggle and nav toggle buttons hidden on desktop (via CSS)
- Improve swipe gesture handling with velocity-based detection
